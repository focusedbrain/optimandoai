# Google Cloud *Desktop* OAuth client secret — next non-comment line. Required by Google on token exchange + refresh alongside PKCE.
# CI: GOOGLE_OAUTH_CLIENT_SECRET or WR_DESK_GOOGLE_OAUTH_CLIENT_SECRET (prepare-google-oauth-resource.cjs writes this file before build).
# Do not commit a real secret; use env in release pipelines.
REPLACE_WITH_CLIENT_SECRET
