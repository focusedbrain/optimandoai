# Build output — the worker bundle is regenerated by buildWorkerBundle.mjs.
# FIX-SPEC A (0021): dist/worker-bundle.cjs + dist/worker-bundle.provenance.json
# are COMMITTED reproducible reference artifacts (V0 = rebuild-and-diff). They are
# force-tracked (`git add -f`) because a parent .gitignore excludes all `dist`
# dirs; once tracked, rebuilds surface as normal diffs. Nothing else under dist/.
dist/
# Rig-local image artifacts (built on the mini-PC, not committed).
*.img
*.ext4
*.cpio.gz
vmlinux
bzImage
rootfs/
