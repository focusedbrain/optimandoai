/** Re-export shared canonical local model identity helpers (single source of truth). */
export {
  canonicalLocalModelName,
  dedupeCanonicalModelNames,
  localModelIdsMatch,
  resolveLocalModelAlias,
} from '../../../../../packages/shared/src/llm/localModelIdentity'
