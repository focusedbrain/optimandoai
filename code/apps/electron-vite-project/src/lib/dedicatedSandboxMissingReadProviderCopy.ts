/**
 * PROMPT 4 — dedicated topology missing read-provider UX copy (renderer).
 */

export const HOST_SANDBOX_READ_ACCOUNT_MISSING_HINT =
  'The sandbox device has no read-only email account — connect one there so it can depackage inbound mail headlessly and deliver to this host inbox.'

export const HOST_SANDBOX_POLL_UNREACHABLE_HINT =
  'Your sandbox device could not run headless depackaging. Check that it is online, the internal handshake is active, and the read account is connected on the sandbox.'

export const SANDBOX_READ_ACCOUNT_SETUP_DETAIL =
  'Connect a read-only email account here so this device can depackage mail headlessly when the host syncs. Results go to the host inbox; only BEAP messages cloned to this device appear in Inbox Clone.'

export const SANDBOX_READ_ACCOUNT_SETUP_TITLE = 'Sandbox read account needed'

export const SANDBOX_READ_ACCOUNT_SETUP_CTA = 'Set up read-only account'
