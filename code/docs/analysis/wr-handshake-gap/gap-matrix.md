# WR Handshake Refactor — Gap Matrix (Phase 2)

Classification vocabulary: `CONFORMANT` / `DIVERGENT` / `MISSING` / `VIOLATION` / `OBSOLETE` (per analysis prompt §5).
Judgment is against the spec as written; sensible-but-off-spec behavior is `DIVERGENT`. Findings marked **[annex-number-provisional]** derive from Annex XII. "L" is always qualified (artifact verification level VII vs Verification Tier L IX) — neither exists in the code today, so no conflation arises in findings.

Paths abbreviated: `hs/` = `apps/electron-vite-project/electron/main/handshake/`, `ii/` = `.../internalInference/`, `main/` = `.../electron/main/`.

---

## Summary counts

| Classification | Count |
|---|---|
| CONFORMANT | 9 (8 Tier-1 + T5) |
| DIVERGENT | 20 |
| MISSING | 36 |
| VIOLATION | 8 |
| OBSOLETE | 3 |
| **Total findings** | **76** (75 Tier-1 rows + T5; Tier-2 rows T1–T4 are foreclosure verdicts, not classified) |

VIOLATIONS (index): C1 per-path formation dialects [VII.4.6], C3 relationship rows from message content [IX.3.1], A6 unknown-field stripping [VII.3.4–3.5], D2 non-admitted external targets reachable [X.3.1/X.3.3], E1 execution authority in grants [VII.10.1], E8 non-silent revocation [VII.10.7.2], F2 partial identity guards [VII.3.10], I3 edge-agent per-device dialect [XI.3-I9, VII.4.6].

---

## A. Handshake core schema & serialization

| # | Req ID(s) | Class | Code location | Evidence (one sentence) | Refactor implication |
|---|---|---|---|---|---|
| A1 | [VII.3] | MISSING | `hs/types.ts:342-450`, `hs/db.ts:25-1230` | There is no frozen signed core; the relationship is a ~60-field mutable record evolved through 72 additive DB migrations, mixing signed-ish wire fields with runtime state, keys and routing. | Introduce an immutable signed-core record type; everything else moves to runtime-state and container structures. |
| A2 | [VII.3.1] (profile) | MISSING | `hs/types.ts` (absent), `hs/capsuleHash.ts:45` | No profile identifier/version exists; the only version is capsule `schema_version` (1\|2). | Profile field lands in the signed core; immutable for relationship lifetime. |
| A3 | [VII.3.1] (initiator_id/responder_id) | DIVERGENT | `hs/types.ts:333-338`, `hs/canonicalRebuild.ts:81-85` | Party identities exist (`PartyIdentity` = email+wrdesk+iss+sub; wire `sender_*`/`receiver_*`) but are only partially covered by the signed hash (accept-only sub fields). | Map party identities into signed core; ensure signature coverage. |
| A4 | [VII.3.1] (ingress_path) | MISSING | `hs/types.ts:342-450` (absent); transient only in `p2p/coordinationWs.ts:490-514`, `p2p/relayPull.ts:314-328` | No `ingress_path` field on the record or capsule; transport tags exist only as transient ingest metadata/log strings. | Add registry-backed `ingress_path` to the signed core, recorded for PoAC, read by nothing. |
| A5 | [VII.3.1] (declarations container) | MISSING | — | No generic `[{ns, version, payload}]` declarations container or declaration registry exists. | Add container + registry dispatch (needed later for `optirando.decl.capability`, session-binding-key declaration [XI.LB§2.2]). |
| A6 | [VII.3.4–3.5] | VIOLATION | `hs/canonicalRebuild.ts:163-179, 580-595` | Inbound capsules are rebuilt through an allowlist that silently strips all unknown fields (and hard-rejects a deny-list) — the opposite of preserve-and-ignore with critical-refusal; there is no criticality concept. | Extensions container with byte-stable serialization, preserve unknown non-critical, visible refusal naming the namespace for unknown critical. |
| A7 | [VII.3.6] | MISSING | — | No extension registry exists. | Registry-driven extension handling. |
| A8 | [VII.6.1.3], canonical-form signing discipline (cf. [XII.5]) | DIVERGENT | `hs/capsuleHash.ts:64-108`, `hs/signatureKeys.ts:53-71`, `hs/capsuleBuilder.ts:137-139` | Deterministic sorted-JSON hashing exists but only over a fixed field **subset**; Ed25519 signs the 32-byte hash, leaving scopes, `capsulePolicy`, tier signals, X25519/ML-KEM keys, P2P tokens and routing fields outside the signed bytes; no domain-separation tag. | One canonicalization module; signature covers the complete canonical core (minus sig field) with a domain tag. |
| A9 | [VII.3.2] | DIVERGENT | `hs/capsuleBuilder.ts:229-234, 702-709`, `hs/enforcement.ts:252-264` | One `sender_signature` plus an optional accept-time `countersigned_hash` over the initiator's hash — not an ordered detached signature list with per-profile cardinality, and no doubly-signed byte-identical core for org-class relationships. | Signature-list verification parameterized by profile registry record. |
| A10 | [VII.3.1] (nonce/replay) | DIVERGENT | `hs/contextHash.ts:123-125`, `hs/steps/dedup.ts`, `hs/steps/chainIntegrity.ts:42-47` | Nonces are generated and format-validated but production replay protection is capsule-hash dedup + seq monotonicity; the nonce-store verifier is not wired. | Decide: promote nonce store or specify hash-dedup as the replay mechanism of the core. |
| A11 | dead verification path | OBSOLETE | `hs/handshakeVerification.ts` | Full crypto-verify helper (incl. seen-nonce replay, stricter receiver binding) is referenced only by unit tests. | Retire or become the single verifier in the refactor; do not migrate as-is. |
| A12 | anti-rollback groundwork (invariant 8) | OBSOLETE | `hs/steps/contextVersions.ts:6-17`, `hs/db.ts:93-98, 2110-2120` | The `context_block_versions` high-water table exists and is upserted, but the pipeline check is an intentional no-op — dead enforcement. | Reinstate as real high-water enforcement in the new pipeline. |

## B. Profile machinery

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| B1 | [VII.4.1] | MISSING | `hs/types.ts:403-404`, `hs/sandboxTopologyKind.ts:20` | Relationship kinds are hardcoded enums (`internal`\|`standard`, topology kinds, device roles) dispatched by code branches; no data-driven profile registry records exist. | Profile registry (fields, ingress paths, signature cardinality, grant vocabularies, attestation rules) as data. |
| B2 | [VII.4.2] (fail-closed dispatch) | MISSING | `hs/steps/schemaCheck.ts:4-12` | The only unknown-thing refusal is schema_version ∉ {1,2}; there is no profile to dispatch on and hence no unknown-profile refusal. | Fail-closed dispatch: unknown profile/version → visible refusal, no fallback. |
| B3 | [VII.4.2 table] | MISSING | — | None of the four initial registry records exists; `standard` approximates a symmetric personal relationship and `internal` a same-principal device pairing, but neither satisfies the declared cardinalities/attestation rules; `pbeap_publisher` and `org_cross` have no analog. | Seed registry with the four records; map/grandfather existing rows (see migration-and-risk.md, open-questions.md Q2). |
| B4 | [VII.4.3], [VII.4.5] | MISSING | — | No profile-specific structural constraints (org-only offering, attestation-forbidden-for-private) exist. | Encode as registry-record constraints, schema-level. |
| B5 | [VII.4.7] | CONFORMANT | (absence) `hs/*` | No profile upgrade/conversion/migration path exists anywhere; re-establishment always creates a new handshake id. | Keep; add inert `prior_ref` lineage namespace only when needed. |
| B6 | [VII.4.10–4.13] | DIVERGENT | `hs/types.ts:12-31`, `hs/steps/tierSteps.ts`, `hs/tierClassification.ts` | An account-class analog (free/pro/publisher/enterprise) exists but is enforced as receiver-side tier minimums and claim downgrades, not as the two entitlement anchors (registry namespace issuance + structural absence of publishing surfaces). | Re-anchor entitlement checks at registry + authoring surface; consumer role stays class-independent. |
| B7 | [VII.5.2] | MISSING | — | There are no capability declarations, so the declaration-absent path with online R-level gating has no substrate. | Arrives with declarations container + registry client. |

## C. Formation / initiation pipeline

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| C1 | [VII.4.6], [IX.3.1] (one pipeline) | VIOLATION | `hs/initiatorPersist.ts:4-10`, `hs/recipientPersist.ts:42-174`, `hs/ipc.ts:809-871`, `hs/enforcement.ts:411-421`, `hs/steps/internalRoutingCapsule.ts` | Formation runs through at least three dialects (initiator persist that "bypasses the receive pipeline", file-import persist, inbound `processHandshakeCapsule`), plus pervasive semantic branching on `handshake_type` incl. a pipeline step dedicated to it and file import force-setting `internal` (`ipc.ts:853-860`). | Single formation pipeline; admission situation becomes a profile-registry parameter; `ingress_path` becomes log-only. |
| C2 | [IX.3.1] (three capture methods) | MISSING | no scan/assisted-capture code (verified: no camera/BarcodeDetector in electron or extension src) | Only manual entry exists (6-digit pairing code, internal only); WR Code scan and Assisted Code Capture (assisted_email / assisted_discovery) do not exist; the 6-digit code is not the WR Code identifier grammar. | Implement capture-method enum feeding the one pipeline; scan/manual unconditional, assisted policy-gated (rule 6). |
| C3 | [IX.3.1 rules 1–4] | VIOLATION | `hs/enforcement.ts:411-413, 745-829`, `main/email/beapSync.ts:82-104` | An inbound email/relay/WS message whose content is an initiate capsule auto-creates a `PENDING_REVIEW` relationship row with no capture event and no user tap before insert — channel-delivered content parameterizes formation directly. (Whether a pending row counts as "establishing" is open-questions.md Q1; the current path is at minimum a channel-triggered formation stage.) | Invitations become inert until a capture/consent event; recognition yields at most a Connect offer after verification. |
| C4 | [IX.3.1 rules 1–4] (links/nav) | CONFORMANT | `main/main.ts:1250-1287, 2481-2495`, `sidepanel.tsx:347-364` | No hyperlink, deep link, protocol handler, or push payload creates or extends a relationship; deep links only select existing UI state. | Preserve under the refactor; add the IX.12.1 acceptance assertion. |
| C5 | [IX.3.1 rule 5] | MISSING | `hs/types.ts:342-450` (absent); `packages/ingestion-core` `provenanceMetadata.ts:26-40` | Capture/ingress provenance is not a field of the stored relationship or any contract object; it exists only as transient ingest metadata and log lines. | Capture provenance (method + source ref) becomes a first-class signed contract field and consent-preview content. |
| C6 | [IX.3.1 rules 7–9] | MISSING | `main/email/*` (no CPR struct) | No Channel Provenance Record exists; SPF/DKIM/DMARC verdicts are not carried as typed fields of depackaged messages nor surfaced with identical alert semantics at consent surfaces; no Scam Watchdog input contract. | CPR produced in depackaging, consumed read-only, ratchet-only; one shared warning component. |
| C7 | [IX.3.2], [VII.4.15] | MISSING | — | No invitation-class concept (`public_bearer` implemented / `targeted_bound` reserved); initiation mechanics are not orthogonal to a profile axis because neither axis exists. | Initiation-class registry; `targeted_bound` stays a reserved name (correctly unimplemented today). |
| C8 | [IX.3.3] | DIVERGENT | `hs/types.ts:333-338`, `hs/enforcement.ts` accept path | Endpoint identities are bound at accept (sub/iss/email persisted) and no post-formation re-verification is imposed, but there is no Handshake Contract object carrying identity fingerprints, and bearer-until-formation semantics are not modeled. | Identity-lock lands in the contract object at formation. |
| C9 | [IX.3.4] (Hash-Pinned Consent, preview contract, origin binding) | MISSING | `AcceptHandshakeModal.tsx`, `hs/auditLog.ts:15-31` | Consent (Accept) stores no content hash of the preview, no bound-definition hash, no contract-state hash; previews are app-composed UI without hash discipline; no origin-set concept exists. | Consent records bind the three hashes; origin matcher exact-set wildcard-free (Public class). |
| C10 | [IX.3.5], [VII.8.5] | MISSING | `src/auth/discovery.ts:4-13` (OIDC only) | No DNS Discovery Record lookup, no well-known publisher endpoint, no Assessment Record Store, hence no three-way agreement check. | Dual-channel verifier + append-only assessment store (also Tier-2 connector seam). |
| C11 | [VII.5.6–5.7] | MISSING | — | No Fast-Path consent, no hash-pinned first-artifact comparison, no all-green collapse predicate. | Arrives with public_bearer profile work. |
| C12 | [VII.4.8–4.9] | MISSING | `orchestratorModeStore.ts:83-87` | Pairing codes are plaintext 6-digit strings; no sealed (X25519/HKDF/AES-GCM) red-code payloads exist; (no color-as-security copy exists either, trivially). | Sealed code payloads per profile keys when red codes arrive. |
| C13 | [VII.10.5.5] (no auto-accept) | CONFORMANT | grep-verified absence; `AcceptHandshakeModal.tsx` | No auto-accept toggle exists anywhere; activation always requires the Accept tap. | Preserve; assert structurally in tests. |
| C14 | `skipConsentForAutomation` | OBSOLETE | `handshake-overrides.ts:30, 178` | A policy-schema flag that would name an auto-consent concept exists with no runtime consumers. | Remove; its existence contradicts the spirit of [VII.10.5.5] if ever wired. |
| C15 | [IX.3.6] | DIVERGENT | `hs/p2pTransport.ts`, `p2p/p2pConfig.ts` | Relay-default with direct-P2P as configuration exists, but tier selection is ad-hoc config, not an operator-policy-governed privacy-tier model, and publisher-side forcing is not structurally excluded. | Model transport tiers as local policy; publisher cannot lower. |

## D. Governance-class routing

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| D1 | [X.3.1] (exactly three classes) | DIVERGENT | `hs/types.ts:403-404`; inventory §3 | Two ledger kinds (`internal`/`standard`) exist; client↔relay is OIDC infrastructure, client↔local-runtime is ambient, external services are ambient config — there is no registry typing every counterparty into exactly one governance class. | Governance-class registry over the profile machinery; "Internal" as admission situation of the one mechanism (also [XII.4] **[annex-number-provisional]**). |
| D2 | [X.3.1], [X.3.3] (non-admitted unreachable) | VIOLATION | `hs/aiProviders.ts:519-590`, `hs/llmStream.ts`, `main/email/gateway.ts:370-434` | Cloud model APIs and email providers are reachable as authorized targets from ambient configuration + stored credentials with no admission artifact; "access without a capability" is representable and routine on the host side. | External Service Admission artifacts + egress resolution only via admitted-target registry. |
| D3 | [X.3.1] (formation authority, no self-admission) | DIVERGENT | `hs/topologyAutoWire.ts:113-161`, `ii/hostAiRemoteInferencePolicyResolve.ts:134-138` | Internal handshake formation itself is consent-gated (typed pairing code + Accept), but post-ACTIVE capability attachment is automatic: topology auto-wire adds `linked[]` routing and Host-AI remote inference defaults to allow when the user choice is `unset`. | Capability-set admission becomes its own hash-pinned policy/consent event; no default-on standing capabilities. |
| D4 | [X.4] (sandbox inherits nothing) | DIVERGENT | `main/email/roleScopedTokenStore.ts:17-20`, `main/critical-jobs/remote/serialize.ts:71-75`, `main/sandbox/sandboxOutboundPolicy.ts:209-224` | Credential-crossing is prohibited (INV-2) and sandbox egress is deny-by-default allowlisted, but the sandbox gains a standing Host-AI capability from pairing default-allow rather than per-operation capabilities. | Requester-class model [X.5]: non-resident components always per-operation capability. |
| D5 | [X.3.3] (ESA artifact) | MISSING | `main/email/gateway.ts`, `main/email/types.ts:274-410` | No unilateral signed versioned admission declaration exists for any non-WR service: no endpoints/identity-anchor declaration, no per-direction data-class ceilings, no Invocation Assurance Classes, no per-invocation authorization. | ESA as a governed artifact type sharing the directional grammar; email-accounts.json becomes input material, not authority. |
| D6 | [X.3.2] (directional capability sets) | MISSING | `hs/types.ts:35` (`SharingMode`), `hs/steps/sharingMode.ts` | The only directionality is the one-bit `receive-only`/`reciprocal` sharing mode; no independent ingress/egress capability-set structures with default-empty semantics exist on any relationship. | One shared per-direction schema consumed by contracts and admissions; undeclared direction = empty set = deviation on flow. |
| D7 | [X.5] | MISSING | — | No seven-field capability grammar (regions, operation, recipient, purpose, measures, validity, revocation) governs graph/context access; access is gated by `effective_policy.allowedScopes` and vault state. | Capability-object API in front of context/graph access; requester classes 2–3 produce Boundary Event Records. |
| D8 | [X.10.1–10.2] | MISSING | `sealed-storage/index.ts:157-184` (RAM-only), diagnostic logs | No sealed, hash-chained Boundary Event Records exist for crossings; the closest artifacts are console diagnostics, mutable ingestion audit rows, and non-durable tamper events. | BER writer with per-domain chain, unified with execution receipts where crossings are consequences. |
| D9 | [X.7.8] (policy anti-rollback) | MISSING | `ii/hostInferencePolicyStore.ts` | Local policy stores are unversioned unsigned JSON; there is no signature verification or high-water version check before enforcement. | Signed local policy + per-object high-water version, verify-before-enforce. |

## E. Grant / rights model

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| E1 | [VII.10.1], [VII.2.6] | VIOLATION | `main/enforcement/authorizeToolInvocation.ts:52-59, 121-124`, `main/execution/executeToolRequest.ts:71-80` | Execution authority is grantable: a process-global `GRANTED_TOOLS` set authorizes tool execution under an ACTIVE handshake without a distinct per-execution human consent tap producing a PoAE record. | Grant type system loses every execute variant; single consent-gated execution gate; per-tap PoAE. |
| E2 | [VII.2.7] | DIVERGENT | `hs/steps/scopePurpose.ts:5-31` vs `main/email/beapEmailIngestion.ts:1019-1048` | Handshake capsules are scope-filtered in-pipeline before persistence, but BEAP inbox messages are admitted on a vault capability check only — no per-relationship delivery-right filter runs before user visibility. | Receiver-enforced admission filter as the first ingress stage for all deliveries; off-scope blocked pre-visibility, logged, revoke offer. |
| E3 | [VII.10.2–10.3] | MISSING | — | Delivery rights are not scope-bound objects and delivered items carry no reference to the grant they were delivered under. | Grant-ref provenance field on delivered items. |
| E4 | [VII.10.5, 10.5.1–10.5.4], [VII.10.4.1–10.4.2] | MISSING | `hs/types.ts:39-45` (`ActionType`) | Standing rights are coarse unpinned flags (`read-context`, `cloud-escalation`, …), not standing action scopes pinning template leaf hashes, typed effect-vocabulary entries, payload schemas and constraints, with separate consent screens and validate-before-render. | Scope objects with pinned hashes + per-payload verification; effect vocabulary registry. |
| E5 | [VII.10.6] | MISSING | `hs/auditLog.ts:9-68` | Grant creation/modification produces no PoAC record; only mutable `audit_log` rows exist. | PoAC writer on grant lifecycle (see area H). |
| E6 | [VII.10.7.1] | CONFORMANT | `hs/db.ts:959-964, 2277-2287`, `hs/enforcement.ts:1073` | Active trust is unlimited-until-revoke (v52 made calendar expiry a no-op; `expires_at` cleared on ACTIVE); residual defensive check (`enforcement.ts:665-671`) and 7-day pending timeout noted (open-questions.md Q7). | Preserve as the ground state. |
| E7 | [VII.10.7.4] | CONFORMANT | `hs/steps/ownership.ts:43-52`, revoke-repair tests | Re-handshake after revocation starts with a new handshake id and empty rights; nothing revives automatically. | Preserve; optional restore-proposal UI later. |
| E8 | [VII.10.7.2] | VIOLATION | `hs/revocation.ts:108-170` | Local revocation enqueues a signed revoke capsule to the counterparty — a publisher-observable signal, where the spec requires unilateral, immediate, **silent** revocation with client-side enforcement only. | Revocation becomes local-only enforcement (ingress filter kills deliveries pre-visibility); peer notification removed or re-specified by the author. |
| E9 | [VII.10.7.3] | DIVERGENT | `hs/revocation.ts:78-85`, `hs/db.ts:2358-2363`, `hs/types.ts:155` | Revocation severs history, not just rights: context blocks and embeddings are deleted (ignoring the `onRevocationDeleteBlocks=false` policy default), and permanent delete removes audit rows. | Rights state decoupled from region history; deletion becomes an explicit separate operator action. |
| E10 | [VII.10.8.1–10.8.3] | CONFORMANT | (absence) | `optirando.grant.single_use` / `.ttl` are not implemented — correct, they are reserved names to be handled only at parse-level criticality [VII.10.8]. | When the extensions container exists, present-but-not-understood limit extensions must refuse, never default to unlimited. |
| E11 | [VII.12.2–12.4] | MISSING | — | No pBEAP request envelope, role asymmetry enforcement, or data-obligations declaration exists (no publisher profile exists at all). | Arrives with pbeap_publisher profile. |
| E12 | [VII.5.5, 5.5.1, 5.5.2], [VII.7.1–7.3] | MISSING | — | No Capability Declaration Block, Manifest Root machinery, verification chain, or artifact verification levels (S/H/H-expired/R/L — "L" here = ledger-anchored artifact level, Annex VII sense) exist. | Out of formation scope but the declarations container (A5) must be able to carry it. |

## F. Identity & SSO binding

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| F1 | [VII.3.8] | CONFORMANT | `hs/ipc.ts:685-690`, `main/ingestion/ipc.ts:104-109`, `hs/ledger.ts:298-306` | No production path creates relationship/grant state without an SSO session: RPC requires session, ingestion requires `ssoSession`, and the ledger opens only from SSO claims. | Preserve; add the K-criterion assertion (no SSO → no relationship, no cache entry, no grant). |
| F2 | [VII.3.10] | VIOLATION | `hs/steps/ownership.ts:10-16`, `hs/handshakeAccountIsolation.ts:15-28`, `packages/coordination-service/src/auth.ts:109-123`, `server.ts:1240-1248`, `ii/policy.ts:7-11` | Identity guards widely use partial comparisons — wrdesk-id-only ownership, OR-logic `sessionMatchesParty` (wrdesk **or** iss+sub **or** email), coordination ack forwarding keyed on JWT `sub` with issuer dropped — the exact defect class of the resolved cross-SSO `beap_ingest_ack` bug persists at other call sites. | One full-claim identity guard helper; audit and convert every ingest/ack/return call site. |
| F3 | [VII.3.9] | DIVERGENT | `hs/steps/receiverBinding.ts:16-34` | Recipient binding exists as email equality at some call sites, but the pipeline step only checks field presence and does not compare against the local session identity. | Pin recipient to the handshake identity in the one pipeline. |
| F4 | [VII.3.11] | MISSING | `hs/capsuleBuilder.ts:549-550`, `hs/signatureKeys.ts:89` | Signing keys are per-handshake TOFU Ed25519 keypairs stored on the relationship row; there is no registry attestation binding keys to SSO identity and no attested rotation events (weak-key rejection exists). | Key custody + SSO attestation model; key history with validity windows. |

## G. Contract objects & persistence

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| G1 | [IX.4] | MISSING | `hs/types.ts:342-450` | There is no two-object model: relationship (long-lived) and versioned content manifest are fused into one mutable record plus context tables; no Site Manifest object exists. | Handshake Contract + (later) Site Manifest under one publisher root; contract changes only via consent events [IX.4.1]. |
| G2 | [IX.4.1] | DIVERGENT | `hs/types.ts:342-450`, `hs/db.ts` update paths | Some contract-minimum analogs exist (party identity, tier signals ≈ attestation level, granted policy), but bound origin set, invitation class, capture method, and Scope Envelope are absent, and the record mutates in place (policies, keys, tokens, seq) without consent events. | Contract mutation only behind consent; rotation = consent event. |
| G3 | [XI.LB§6], [XI.LB§2.2] | MISSING | same as G2 | No hash-stable, byte-identical, versioned handshake object exists that an LBCP could pin — in-place mutation forecloses the Login-Bound machinery (Tier-2 critical seam). | Immutable versioned core + separate runtime state container keyed by handshake hash. |
| G4 | [IX.4.2], [X.7.8], [XI.A.6.3] (anti-rollback) | DIVERGENT | `hs/steps/chainIntegrity.ts:42-63` vs `hs/steps/contextVersions.ts:6-17` | Per-capsule seq/prev_hash anti-replay exists, but no per-object high-water version store rejects validly-signed older policies/manifests, and the context-version check is a no-op. | Persist per-object high-water versions; reject regression fail-closed. |
| G5 | ledger integrity claim | DIVERGENT | `hs/ledger.ts:1-17` vs `:223-228` | `handshake-ledger.db` is documented as hashes/metadata-only but applies the full handshake migration set, so it can hold private keys and inbox tables through that handle. | Decide the ledger's role in the two-store model; stop schema bleed (migration note in migration-and-risk.md). |
| G6 | key custody (cf. [IX.10]) | DIVERGENT | `hs/db.ts` (`local_private_key`, `local_x25519_private_key_b64`, `local_mlkem768_secret_key_b64` columns) | Private signing/agreement keys are stored inline on relationship rows in the vault/ledger DBs rather than in a dedicated client-core key store. | Key store separation before the contract store is made hash-stable. |

## H. PoAC / PoAE / evidence writers

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| H1 | [IX.19.1], [X.10.1] (chained, append-only) | MISSING | `hs/db.ts:119-130, 2362`, `main/retention/retentionJob.ts:73-94` | No evidence log is hash-chained or append-only: `audit_log` has no prev-hash/sequence discipline and is deleted with the handshake; ingestion audit is purged by retention. | New append-only, hash-chained, per-domain evidence chain (Tier-L verifiable in the Annex IX sense — Verification Tier L, local hash chain); existing audit_log frozen read-only. |
| H2 | PoAC (invariant 12, [VII.10.6], [VII.11.7]) | MISSING | grep-verified absence | No PoAC record type exists for relationship/grant lifecycle events. | PoAC writer on formation, grant create/modify, revocation, region creation. |
| H3 | PoAE ([VII.10.1], [IX.19.1]) | DIVERGENT | `apps/extension-chromium/.../poae.ts`, `main/.../dashboardSnapshot.ts` | PoAE-shaped records exist but only embedded in extension-built package JSON inside mutable inbox rows — not a durable sealed chain, and without the 13-field receipt schema. | Executions seal PoAE records into the evidence chain. |
| H4 | [IX.19.2] (Intent Hash) | MISSING | `AcceptHandshakeModal.tsx`, `ConsentDialog.tsx:238-258`, `vault/db.ts:690-698` | No consent surface computes a hash over the preview as presented; consent artifacts store ids/booleans only. | Canonical/hashable preview serialization at presentation time; Intent Hash bound into consent records and receipts. |
| H5 | evidence deletion behavior | DIVERGENT | `hs/db.ts:2358-2363`, `hs/revocation.ts:78-85` | Deleting a handshake deletes its audit rows; revocation deletes blocks/embeddings — history is mutable where the target requires append-only evidence. | Deletion semantics redefined against the append-only chain (digests may remain when content is purged). |

## I. Cross-device

| # | Req ID(s) | Class | Code location | Evidence | Refactor implication |
|---|---|---|---|---|---|
| I1 | [XI.LB§3], [XI.3-I1] (code = identifier + invitation, never authority) | DIVERGENT | `hs/resolvePairingCode.ts:27-76`, `hs/ipc.ts:1987-2032` | The pairing code is identifier-only and resolving/claiming it confers nothing (conformant half), but binding is a human re-type string-equality check rather than a challenge exchange for the second device's own binding. | Cross-device binding via fresh nonce/epoch challenge exchange; code remains invitation-only. |
| I2 | [XI.LB§2.1]-adjacent (challenge mechanics) | MISSING | grep: no split challenge / epoch anywhere in pairing | No nonce-based, single-use, epoch-stamped challenge object exists in any pairing or binding flow. | Design the binding-participant interface now so extension-MVP and integrated client share one contract [XI.B.2]. |
| I3 | [XI.3-I9], [VII.4.6] | VIOLATION | `apps/edge-agent/dist/pairingProtocol.js:23-80`, `edgeIngestorHandshake.js:8-21` | The edge agent implements its own pairing dialect producing `edge_ingestor` pair records outside the handshake ledger — a per-device-class mechanism where device classes must be governance-identical (device-variance wording of XI.4.1 is superseded-pending and grants no license here). | Fold edge-agent pairing into the one mechanism (Internal Handshake admission situation) or retire it. |
| I4 | key/authority transfer | CONFORMANT | `main/device-keys/deviceKeyStore.ts:179-197` | No private keys or authority transfer between devices; per-device X25519 refuses overwrite; peers exchange only public material. | Preserve. |
| I5 | [XI.14] (phone-as-approver), [XI.LB§4–5] lifecycle | MISSING | — | No approver-device enrollment, no consent transport carrying an Intent Hash, no heartbeat/epoch machinery (future scope; must not be foreclosed). | Refactor leaves the consent-record and capability-token schemas able to carry intent hashes and binding references. |

## Tier 2 — forward-compatibility checks (foreclosure verdicts only)

| # | Concern | Req ID(s) | Verdict | Evidence / minimal seam the refactor must leave open |
|---|---|---|---|---|
| T1 | WR Connector server-side contract | [XI.LB§3], [XI.A.17], [VII.8.5], [IX.3.5] | **Not foreclosed** (nothing exists yet) | No DNS/well-known/assessment code exists to conflict. Seams: (a) append-only typed Assessment Record Store (new record types, never schema migration [IX.3.5]); (b) verifier module slot performing the three-way agreement check; (c) manifest namespace able to carry the session-binding-key declaration [XI.LB§2.2] — requires the declarations container (A5). |
| T2 | LBCP / Login-Bound machinery | [XI.LB§2, §4–§6] | **Currently foreclosed** | In-place-mutated `HandshakeRecord` (G3) provides nothing hash-stable to pin. The refactor must deliver an immutable, versioned, hash-stable handshake core; the runtime/session container must be keyed by handshake hash. Heartbeat/epoch not built now. |
| T3 | Mobile / Profile A | [VII.4.12], [VII.14.1], [VII.4.8], [IX.3.1 rule 6] | **Not foreclosed, nothing supports it** | Consumer role is not entitlement-gated today (tier gates are receiver-policy side) — compatible with [VII.4.12]. No Capability Diff exists; when built it must be one shared contract across ingress paths [VII.14.1]. Scan path (C2) must be designed to handle sealed payloads [VII.4.8]. |
| T4 | Coordination Contexts: capability-token fields | [XII.12.6], [XII.9] **[annex-number-provisional]** | **Action lands now** | No structured bilateral capability token exists (P2P tokens are opaque UUID bearer strings; capsules are whitelist-rebuilt, which *strips* unknown fields — the anti-pattern). The new grant/capability token schema introduced by the refactor MUST (a) define optional `context_scope` and `delegation_chain` fields, and (b) adopt preserve-unknown-optional-field parsing plus signature-over-complete-canonical-form so future fields don't break old verifiers. Current `p2p_signal` (preserves unknown non-forbidden keys) is the compatible pattern; `canonicalRebuild` FIELD_RULES is the incompatible one. |
| T5 | Reserved names must NOT be implemented | [IX.3.2], [IX.3.8], [VII.10.8], [VII.6.3] | **CONFORMANT (by absence)** | `targeted_bound`, Credential Attachment envelope, `wr_ad`, `optirando.grant.single_use`/`.ttl`, bridge resolution: none implemented. The refactor must register the names (refusal/criticality handling at parse level) without shipping semantics. |

## Explicitly out-of-scope attachment points (noted only for non-foreclosure)

Site Manifest rendering, WR View / Verified Local Store, Finalizer lifecycle internals, WR Guard classifier internals, WCR execution, Verification Tiers C/X submission, Pointer/Overlay UI: no current handshake structure blocks their later attachment **except** the mutable single-record contract (G1/G3) and the absence of the declarations container (A5), both of which are already Tier-1 refactor items. The workspace's internal-inference invariants (relay carries `p2p_signal` only; no inference on the coordination relay; metadata-only logs) are compatible with, and anticipate, [X.0]'s no-new-enforcement-layer rule — the refactor must not regress them.
