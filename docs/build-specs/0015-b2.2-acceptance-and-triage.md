# B2.2 — ACCEPTED. Rulings + one off-rig task (docs/build-specs/0015)

Commit this as `docs/build-specs/0015-b2.2-acceptance-and-triage.md`, update the README index, then execute Part 2.

## Part 1 — Rulings

1. **Deviation #2 (`threadingHints`) is APPROVED.** It is the correct application of spec 0013 §2.2 ("IMAP threads from the guest's result"), not an extension beyond it. Update `0014`'s deviation entry to record approval; the field stays.
2. **Notification-ordering delta is SIGNED OFF**, recorded with this framing: *flag-on, OS notifications display attacker-controlled content (subject/sender) only after it has passed the isolation boundary; pre-depackage notifications are generic by design.* This is a security-aligned behavior, not an accepted regression. Note it as such in `0014` and in any future user-facing docs.
3. B2 + B2.1 + B2.2 are jointly **code-complete, not accepted**; the V-series (`0009`, evidence → `0011`) remains the sole acceptance gate. Flags unchanged: depackage cutover OFF everywhere; B1 validation cutover stays in soak.

## Part 2 — Test-debt triage (off-rig, do now; this is NOT part of B2)

The whole-repo run carries 28 pre-existing failures (`internalInference`, `llm/diagnostics`, `sealed-storage`, extension `beap-builder`) confirmed identical on base `96981252`. This debt taxes every future build with innocence-proving archaeology, and `internalInference` is the template Build C's `RemoteHandshakeExecutor` will be modeled on — its tests must be trustworthy before that build starts.

Scope (triage, not refactor):
1. **Classify each failure:** (a) stale test (asserts outdated behavior), (b) genuinely broken code, (c) environment/timing (the parallel-load class already seen twice), (d) orphaned (tests for untracked/dead code).
2. **Fix only the cheap ones now:** class (c) gets the established timeout treatment; class (a) gets updated only where the correct current behavior is unambiguous from code; class (d) gets skipped with a reason annotation referencing this spec.
3. **Class (b) — broken code — is NOT fixed in this task.** Each instance gets a short entry (file:line, symptom, suspected cause, blast radius) in the report. **Exception requiring detail: every `internalInference` failure gets a full diagnosis** (is the request/result-over-handshake plumbing itself broken, or only its tests?), because Build C's design assumption — "the internal-inference pattern is live and proven" — comes from the original architecture analysis and must be re-verified before Build C is specced.
4. **Report** as the next numbered file: classification table, what was fixed, the class-(b) register, and an explicit verdict line: *"internalInference plumbing: sound / broken / partially broken"* with evidence. Zero changes to seam, email-path, or flag-gated B2 code; report contradictions before absorbing them.

## Carried forward (unchanged)

Rig V-series V0–V7 (the next milestone; bundle hash now `f7310ffd…` per `0009` V0). Then: B2 acceptance → `decrypt-qbeap` → Build C (gated on the internalInference verdict above) → fetch relocation → B1 default-flip cleanup.
