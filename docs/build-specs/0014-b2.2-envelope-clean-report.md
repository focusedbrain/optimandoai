# wrdesk — B2.2 Report: Fully-Clean Envelope Path (docs/build-specs/0014)

**Status:** CODE-COMPLETE, off-rig. Implements spec `0013`. The last orchestrator-side
parse retained flag-on (the IMAP/Gmail envelope-metadata parse, deviation `0012`
§3.1) is removed. Flag posture unchanged: `WRDESK_SEAM_DEPACKAGE_CUTOVER` OFF
everywhere. The V-series (`0009`) remains gated and now produces an **unqualified**
invariant-0 claim once green on the rig.

---

## Commit map

| Commit | Scope |
| --- | --- |
| `39ce6328` | Guest `displayEnvelope` (RFC 2047 decode, address parse, caps, degradation) + ParseOut/result-union integration + both producers + spec `0013`; decoder/caps/degradation unit tests + equivalence corpus envelope leg |
| `936dac93` | Orchestrator provider-native bookkeeping flag-on (IMAP `BODY[]`-only, Gmail `format=raw`-only); `messageRouter` envelope from result; header-parse guard tripwires + tests |
| `fc3263f4` | In-guest `threadingHints` (Message-ID/In-Reply-To/References) for IMAP threading/MOVE; Outlook `internetMessageId`; threading unit + equivalence tests |
| `27e92efa` | **Hygiene (separate):** bump `b5ExtensionMerge` §G.1 suite timeout (pre-existing flake) |
| *(uncommitted in this report commit)* | `0009` V0/V3/V4 edits + this report + README index |

Worker bundle is a git-ignored reproducible artifact (rebuilt via
`buildWorkerBundle.mjs`); its hash is recorded below and in `0009` V0.

---

## §1 — Guest display envelope

- **`displayEnvelope.ts`** (new, pure: `Buffer` + global `TextDecoder` only):
  - `decodeHeaderText` — RFC 2047 encoded-word decode (B/Q; per-word charset via
    `TextDecoder`; adjacent-word whitespace collapse per §6.2). Any encoded word that
    cannot be decoded (unknown charset, malformed) → the field degrades to the raw
    string, `degraded:true` — never a partial mix.
  - Address-list parsing (`splitAddressList` respects quotes + angle brackets, so a
    quoted-comma display name does not split), name decode + caps.
  - Date normalization (ISO 8601 when parseable; otherwise capped raw + degraded).
  - C4 caps in `ENVELOPE_CAPS` (subject/name/email/date length, recipient count).
  - Two producers: `buildEnvelopeFromHeaders` (RFC822) and `buildEnvelopeFromFields`
    (Graph — provider strings still treated as untrusted: decoded [no-op when already
    decoded] + capped identically, so the two forms converge).
- **`ParseOut` + the typed result union** gain `displayEnvelope`; populated by
  `hardenedParse` (headers) and the Outlook walker (Graph fields). `buildResultFromParse`
  threads it onto all three `ok` variants. **SafeText subject is now the decoded
  subject in both forms** (was the raw encoded-word header on the RFC822 path) — a
  latent inconsistency fixed in passing; this is also what the live gateway showed.
- **Degradation, not risk routing (§1.2):** a failed/ambiguous decode does NOT
  quarantine. The field shows the capped raw string and is listed in
  `degradedFields`; content processing proceeds. Rationale recorded in the module
  header: nothing structural crosses the boundary unparsed.

## §2 — Orchestrator provider-native bookkeeping (flag-on)

- **IMAP** (`providers/imap.ts`): flag-on, `stream.once('end')` skips `simpleParser`
  entirely and builds the message from provider-native attrs (UID, INTERNALDATE →
  `date`, flags) + the opaque `BODY[]` buffer as `rawRfc822`. `openBox(...,true)` is
  READ-ONLY ⇒ `BODY.PEEK[]` semantics (`\Seen` never set). No `ENVELOPE`/header parse,
  no local header decode. Flag-off path is the original `simpleParser` branch,
  **byte-identical**.
- **Gmail** (`providers/gmail.ts`): flag-on, a single `format=raw` fetch yields
  id/threadId/labelIds/internalDate + opaque raw; `buildRawGmailMessage` assembles the
  message with no header/body walk. Flag-off path = `format=full` + `parseGmailMessage`,
  unchanged.
- **Outlook**: payload per the existing opaque-input union (structured-json default);
  envelope fields (`from`/`toRecipients`/`ccRecipients`/`replyTo`/`receivedDateTime`/
  `internetMessageId`) shipped opaque and decoded in-guest by the walker.
- **`messageRouter`**: plain + carrier rows take from/to/cc/subject/date from the
  guest `displayEnvelope`; bookkeeping date is provider-native (INTERNALDATE/
  internalDate/receivedDateTime), falling back to the guest-decoded `Date` header.

### §2.2 Threading/dedupe audit (findings → resolution)

| file:line | key | flag-off | flag-on resolution |
| --- | --- | --- | --- |
| `syncOrchestrator.ts:240–248` | `headerBlock` from `detail.headers.messageId/inReplyTo/references` | provider-parsed headers | providers emit `headers:{}` flag-on ⇒ `headerBlock` undefined; **no orchestrator-parsed header used** |
| `syncOrchestrator.ts:725–747` | dedupe `existingIds.has(msg.id)` | provider-native id | **provider-native** (IMAP UID / Gmail id / Graph id) — no change needed |
| `syncOrchestrator.ts:627–665`, `:813` | `seen` set / `lastUidSeen` | provider-native id/UID | **provider-native** — no change needed |
| `messageRouter.ts` (`writePlainSeamInbox`) | `imap_rfc_message_id` ← `rawMsg.headers.messageId` | parsed RFC Message-ID | keys on **guest-derived `threadingHints.messageId`** (RFC822: from headers in-guest; Graph: `internetMessageId`) |
| `messageRouter.ts` (carrier re-entry) | pipeline-2 `headers.messageId` | parsed | injected from `result.threadingHints.messageId` |
| `providers/imap.ts` (was `threadId: parsed.messageId`) | IMAP "thread id" | RFC Message-ID (locally parsed) | flag-on `threadId: undefined`; IMAP has no native threading and **threads post-depackage on the guest result** |
| `providers/gmail.ts` `:1399`, `providers/outlook.ts` `:1213` | `threadId` | `raw.threadId` / `conversationId` | **provider-native** — no change needed |

**Resolution:** the one residual hiding place (IMAP RFC Message-ID for threading/MOVE)
is now derived **in-guest** as `threadingHints` (capped, never RFC 2047 decoded) and
carried on the result, so flag-on no orchestrator code parses a header for a threading
key. Gmail `threadId` / Graph `conversationId` were already provider-native.

### §2.3 Notification ordering (product-visible delta — for sign-off)

Flag-on, a "new message" notification that fires before depackage completes has **no
subject/sender** (the orchestrator no longer parses headers up-front). Subject/sender
**backfill** when the depackage job returns the `displayEnvelope`. Net delta: a brief
window where a newly-arrived mail shows a generic "new message" before its
subject/sender populate. No correctness impact; flag-off unchanged. **Sign-off
required** on this UX delta before enabling the flag in production.

## §3 — Storage, guard, parity

- **Storage:** envelope columns populate from the depackage result flag-on; flag-off
  unchanged + byte-identical (verified: full email suite green flag-off).
- **Guard:** `assertNoInlineParse` tripwires added at the IMAP `simpleParser` and Gmail
  header-walk entry points. The guard now covers **body, classification, AND headers** —
  the complete invariant-0 surface. Flag-off these branches run unchanged; flag-on they
  are unreachable and would fail closed (`E_INLINE_PARSE_FORBIDDEN` → quarantine via the
  mapping table).
- **Parity:** the dual-form equivalence corpus
  (`providerStructuredWalker.equivalence.test.ts`) gains a rich-envelope pair asserting
  **envelope + threading equality** (encoded-word RFC822 subject/from/addresses/
  Message-ID == decoded Graph fields). Unit corpus
  (`displayEnvelope.test.ts`) covers encoded-word B/Q + multi-charset, quoted-comma
  address split, oversized subject/recipient caps, undecodable subject → degraded,
  invalid date → degraded, threading caps/split.

## §4 — Hygiene

`b5ExtensionMerge` §G.1 file timeout bumped to 30 s (`vi.setConfig`), mirroring
`hardening.test.ts` (`b408278f`). Pre-existing parallel-load flake; no production code
touched. Committed separately (`27e92efa`).

---

## Bundle

```
sha256(rig/dist/worker-bundle.cjs) = f7310ffdb081275921de5c692923ea94fb880d5da21d4def47264d13bc22b6d7
```

Supersedes the B2.1/D6 hash `cb04ae5150daee06fb8d27d776492421445fb444354c14ac85fed37b0155eead`.
`0009` V0 updated. Bare-node smoke (off-rig, no crosvm):
- `rfc822` depackage-email → `ok:true`, `displayEnvelope` = decoded subject "Tschüss",
  from "Renée", ISO date, `threadingHints.messageId` = `<smoke@x.com>`.
- `provider-structured-json` (Outlook) → identical decoded subject + Message-ID.
- B1 `depackage` path → `ok:true` (unchanged).

## Proof obligations (off-rig)

- **Flag OFF, full suites:** the B2.2-touched suites (`depackaging-microvm`,
  `critical-jobs`, `email`) are **green flag-off — 412 passed, 67 skipped**, zero
  failures. The whole-repo run shows pre-existing failures **only** in unrelated areas
  (`internalInference`, `llm/diagnostics`, `sealed-storage` zeroize property,
  extension `beap-builder`); these were confirmed failing identically on the base commit
  `96981252` **before any B2.2 change** — zero new failures attributable to B2.2.
- **Flag ON (dev harness, in-process):** exercised via the bundle smoke + unit corpus —
  guest-derived envelopes per form; forced/undecodable header → field degrades to
  placeholder with `degradedFields` marker and content still processes (not quarantine);
  malformed-structure (walker) → `E_AMBIGUOUS_STRUCTURE` quarantine. Live-provider
  flag-on (real IMAP/Gmail/Outlook fetch path) is **rig-only** (V3/V4); the provider
  FETCH refactors are flag-gated and cannot be validated off-rig.
- **Guard:** header-parse-surface guard tests prove inert flag-off, fail-closed flag-on.

## Deviations (reported, not absorbed)

1. **Provider FETCH refactors are flag-gated and rig-validated, not off-rig.** The IMAP
   `BODY[]`-only and Gmail `format=raw`-only paths run only flag-on, which has no
   off-rig harness with a live mailbox. They are verified by inspection + the guard
   tripwires + flag-off byte-identity; **V3/V4 on the rig are the acceptance gate.**
2. **`threadingHints` — APPROVED (ruling `0015` §1.1).** Originally flagged as a possible
   extension beyond spec §1.1's literal display-field list (Message-ID is a threading key,
   not a display field). Ruling `0015` confirms this is the **correct application of spec
   0013 §2.2** ("IMAP threads from the guest's result"), not an extension. Message-ID/
   In-Reply-To/References are derived **in-guest** as a `threadingHints` field on the
   result; the field stays. Header handling stays out of the orchestrator while threading/
   MOVE is preserved.
3. **Notification-ordering delta — SIGNED OFF (ruling `0015` §1.2).** Recorded framing:
   *flag-on, OS notifications display attacker-controlled content (subject/sender) only
   after it has passed the isolation boundary; pre-depackage notifications are generic by
   design.* This is a **security-aligned behavior, not an accepted regression** — the
   generic pre-depackage notification is the secure default, and subject/sender backfill
   only once the guest has decoded the envelope. Carry this framing into any future
   user-facing docs.
4. **DB-backed flag-on storage parity** (envelope columns end-to-end) is asserted by the
   unit + equivalence corpus and the `messageRouter` wiring; the sqlite-backed
   end-to-end legs remain `skipIf(!Database)` off-rig, same as B2/B2.1.

## Out of scope (per spec 0013 §5)

Everything in 0007 §7, the `/$value` preference, the V-series itself, any flag default
change.
