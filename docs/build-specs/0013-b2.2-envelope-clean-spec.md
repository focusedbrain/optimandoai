# wrdesk — B2.2: Fully-Clean Envelope Path (docs/build-specs/0013)

**Type:** Build. Off-rig. Removes the last orchestrator-side parse retained flag-on (the IMAP/Gmail envelope-metadata parse, deviation recorded in `0012` §3.1), so the V-series produces an **unqualified** invariant-0 claim in one pass. Commit this spec as `docs/build-specs/0013-b2.2-envelope-clean-spec.md`; report lands as `0014`. Flag posture unchanged: `WRDESK_SEAM_DEPACKAGE_CUTOVER` OFF everywhere; the V-series remains gated until B2.2 is green.

**Ruling context (binding):** header parsing is untrusted-structure parsing (RFC 2047 encoded-words, malformed addresses, header folding). Per INV-7 the flag-on path may not retain it. The 0007 phrase "minimal envelope metadata the provider supplies outside the MIME body" means provider-native fields only (ids, folder, internal date, size) — never locally-parsed headers.

---

## 1. Guest: display envelope in the typed result

1. Extend `ParseOut` (and the typed result union) with `displayEnvelope`: subject, from, to, cc, reply-to, date — decoded (RFC 2047 etc.) and normalized **inside the guest**, with C4-style caps (max field length, max recipient count). Both producers populate it: the RFC822 parser and `providerStructuredWalker` (Graph fields are still treated as untrusted strings; cap and normalize identically).
2. **Envelope decode failure does NOT quarantine the message.** On a failed/ambiguous decode, the affected field degrades to a length-capped literal placeholder (the raw string shown as-is, marked degraded) and content processing proceeds normally. Rationale recorded with the code: nothing structural crosses the boundary unparsed, so this is display degradation, not risk routing. Add a `degradedFields` marker to `displayEnvelope` so the UI can indicate it.
3. Rebuild the worker bundle; record the new sha256 in `0009` V0 (superseding the D6 hash) and refresh the V1/V2 image-refresh note.

## 2. Orchestrator: provider-native bookkeeping only (flag-on)

1. **IMAP:** sync uses `UID`, `INTERNALDATE`, `RFC822.SIZE`, and `BODY.PEEK[]` for the opaque payload. No `ENVELOPE`/header FETCH parsing, no local header decoding of any kind. **Gmail:** `format=raw` for payload; ids/`internalDate`/labels from the list/metadata calls used as opaque identifiers only. **Outlook:** Graph `id`/`receivedDateTime`/folder; payload per the existing opaque-input union.
2. **Threading/dedupe audit (mandatory, report the findings):** identify every place threading or dedupe keys on locally-parsed headers (`Message-ID`, `References`, `In-Reply-To`). Flag-on, these must come from provider-native thread ids (Gmail `threadId`, Graph `conversationId`) or from the guest's `displayEnvelope`/result (IMAP, which has no native threading, threads post-depackage). No exceptions; this is the most likely hiding place for a residual parse.
3. **Notification ordering (product-visible delta — record for sign-off in the report):** flag-on, a new-mail notification fired before depackage completes may be generic ("new message"); subject/sender backfill when the job returns. Keep the delta note short and explicit.

## 3. Storage, guard, parity

1. Envelope columns populate from the depackage result flag-on; flag-off unchanged and byte-identical.
2. **Guard extension:** add the envelope/header-parse entry points to `inlineParseGuard` (`E_INLINE_PARSE_FORBIDDEN`, same flag-on semantics, same quarantine routing). After this, the guard covers body, classification, and headers — the complete invariant-0 surface.
3. **Parity:** corpus including encoded-word subjects (B64/QP, multiple charsets), malformed/oversized headers (→ degraded placeholders, message still processed), address-list edge cases. Flag-on envelope output must equal today's parsed envelope on the well-formed corpus; degraded cases follow §1.2. Extend the dual-form equivalence corpus (RFC822 vs Graph JSON) with envelope equality.
4. Update `0009`: V3's claim wording becomes unqualified (no header exception); V4 gains the envelope-parity legs per provider; note the superseded bundle hash.

## 4. Hygiene (separate, clearly-labeled commit)

Bump the `b5ExtensionMerge` §G.1 suite timeout (same treatment as `hardening.test.ts` in `b408278f`) — pre-existing parallel-load flake, unrelated to B2.2.

## 5. Proofs (off-rig) and process

Flag off: full suites green, zero diff. Flag on (dev harness, in-process): IMAP/Gmail/Outlook fixtures complete end-to-end with guest-derived envelopes; forced header parse → guard fires → quarantine; degraded-decode fixture → placeholder + normal content processing. Report `0014`: commit map, threading-audit findings (file:line → resolution), the notification-delta note for sign-off, parity evidence, new bundle hash, deviations reported not absorbed. Out of scope: everything in 0007 §7, `/$value` preference, the V-series itself, any flag default change.
